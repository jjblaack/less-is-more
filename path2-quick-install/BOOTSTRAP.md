# 一键快装路径 · Bootstrap Prompt

> 使用方式：在**分发包根目录下**启动 Claude Code，然后把下面 `---` 之间的整段内容复制粘贴给 Claude。

---

你好 Claude。我拿到了一份现成的 harness 分发包，想走"一键快装"路径，把这套 harness 快速装到我的 `~/.claude/` 里先用起来。

## 我把材料都放在你当前工作目录下了

- `path2-quick-install/INSTALL.md` —— 安装说明（给我看的）
- `path2-quick-install/agent-steps.md` —— **你的执行步骤参考，里面列了所有你必须问我确认的检查点**
- `path2-quick-install/claude-home/` —— 待复制到 `~/.claude/` 的整包
  - `CLAUDE.md` / `settings.json` / `agents/` / `commands/` / `hooks/` / `skills/`
  - CLAUDE.md 里的身份段已被替换成 `{{你的名字}}` 占位符，装完你会提醒我替换

## 我希望你遵守的纪律

1. 严格按 `agent-steps.md` 的流程走，所有带 🛑 的检查点必须停下来问我
2. **协作铁律**：
   - **谋而后动**：接需求先复述理解 → 等我确认 → 给方案 → 等我确认 → 才动手
   - **缺信息就问**：不清楚的事主动问我，不要脑补
   - **该问就问**：涉及删除/覆盖我已有文件的操作，**必须先列清楚 + 等我明确授权**
3. **装前必问**：
   - 我是新用户还是老用户？
   - `~/.claude/` 当前有哪些内容？哪些是我的、哪些能动？
   - 老用户的处理策略选 A（全替换）/ B（合并保留）/ C（纯增量）/ D（暂停）？
4. **装前必备份**：改动前先 `tar -czf` 整个 `~/.claude/` 快照
5. **settings.json 永远增量合并**：不删我任何已有配置
6. **装后必提醒**：CLAUDE.md 里的 `{{你的名字}}` 等占位符需要我替换，Soul 可以之后再补

## 请执行的第一步

Read 下面这 2 个文件：
1. `path2-quick-install/agent-steps.md`
2. `path2-quick-install/INSTALL.md`

读完后回复我：
- 你理解的本次任务目标是什么
- 你准备分哪几个阶段推进
- 第一个要问我的问题是什么（通常是新老用户识别）

**不要直接开始装。** 我看到你的理解和计划之后，会告诉你能不能开始。
